export enum UserRole {
  ADMIN = 'admin',
  PLAYER = 'player',
  MANAGER = 'manager',
  VIEWER = 'viewer',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum Theme {
  LIGHT = 'light',
  DARK = 'dark',
}